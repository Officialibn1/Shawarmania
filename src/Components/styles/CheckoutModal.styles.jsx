import styled from "styled-components";

export const StyledCheckoutModal = styled.div`
    
`

export const Div = styled.div`
    margin-bottom: 8px;

    label{
        font-size: 1em;
        letter-spacing: 2px;
        opacity: .8;
    }

    input{
        border: 1.5px solid rgba(0, 0, 0, 0.504);
        padding: 0 10px;
    }
`

export const Button = styled.button`
    padding: 10px;
`